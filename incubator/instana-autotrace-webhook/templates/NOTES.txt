The Instana AutoTrace WebHook is installed.

It will automatically instrument:
{{- if .Values.autotrace.libinstana_init.enabled }}
  - Node.js, .NET Core, Ruby & Python applications via LD_PRELOAD
{{- else }}
  {{- if .Values.autotrace.nodejs.enabled }}
    - Node.js applications
  {{- end }}
  {{- if .Values.autotrace.netcore.enabled }}
    - .NET Core applications
  {{- end }}
  {{- if .Values.autotrace.ruby.enabled }}
    - Ruby applications
  {{- end }}
  {{- if .Values.autotrace.python.enabled }}
    - Python applications
  {{- end }}
{{- end }}
{{- if .Values.autotrace.ingress_nginx.enabled }}
  - Ingress NGINX
    DEPRECATION NOTICE: NGINX Tracer has announced deprecation for NGINX versions earlier than 1.24.0 and NGINX Plus versions earlier than R30. These deprecated versions will be supported until April 1, 2027. Going forward, Instana will deprecate EOL NGINX versions after two years of their official end-of-life date. After this date, older versions of NGINX Tracer will remain available but will not receive maintenance updates. To ensure optimal performance and security, upgrade to a supported NGINX version.
{{- end }}

Logs for the Instana AutoTrace WebHook are available via:

  kubectl logs -l app.kubernetes.io/name={{ .Release.Name }} -n {{ .Release.Namespace }}

Ensure to redeploy the Kubernetes resources for mutation to take effect.

{{- if .Values.autotrace.ibmmq.enabled }}

WARNING: IBM MQ Support Deprecation
DEPRECATION WARNING: Instana Autotrace webhook support for IBM MQ is deprecated. Effective 1 May 2027, Instana will officially end support and declare it end of life (EOL).
This feature is replaced by IBM MQ operator. See https://ibm.biz/~iOcjBE7GN for more details.
{{- end }}

{{- if .Values._warnings }}
WARNINGS:
{{- range .Values._warnings }}
  - {{ . }}
{{- end }}
{{- end }}
